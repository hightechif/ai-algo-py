# 03 KNN Tasks

## Implementation Checklist
- [x] 1. Initialize `module_1_foundations/03_knn/` directory structure.
- [x] 2. Create theoretical documentation (`README.md`) featuring proper GitHub block math equations with variable escaping per `.clinerules`.
- [x] 3. Implement pure NumPy KNN (`scratch.py`) forcing static `typing`, accommodating both `task` options (classification vs regression) and distance `metric` formulations (euclidean vs manhattan).
- [x] 4. Implement scikit-learn wrapper (`framework.py`) wrapping the KNeighborsClassifier.
- [x] 5. Create Jupyter Notebook tutorial (`tutorial.ipynb`) evaluating multi-class accuracy on the real-world `Iris` dataset, and visually exploring the $K$ variation effects.

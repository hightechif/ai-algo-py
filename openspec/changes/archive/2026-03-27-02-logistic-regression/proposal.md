# 02 Logistic Regression Proposal

## Goal
Implement Algorithm 02: Logistic Regression for binary classification in Module 1.

## Scope
- Mathematical documentation of the Sigmoid function and Cross-Entropy Loss.
- A pure Python/NumPy implementation (`scratch.py`).
- A scikit-learn framework wrapper (`framework.py`).
- A Jupyter Notebook tutorial (`tutorial.ipynb`) comparing both implementations.

## Dataset
Following the new rule in `.clinerules`, we will use the Breast Cancer dataset from `sklearn.datasets` to evaluate binary classification accuracy.

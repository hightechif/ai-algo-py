# 04-naive-bayes Tasks

## Implementation Checklist
- [x] 1. Initialize `module_1_foundations/04_naive_bayes/` directory structure.
- [x] 2. Create theoretical documentation (`README.md`) detailing Multinomial Naive Bayes using GitHub block math and emphasizing the naive conditional capability assumption.
- [x] 3. Implement pure NumPy Multinomial NB (`scratch.py`) featuring static typing. Emphasize Laplace Smoothing parameter scaling and log-probabilities logic.
- [x] 4. Implement scikit-learn equivalent reference wrapper (`framework.py`).
- [x] 5. Create Jupyter Notebook tutorial (`tutorial.ipynb`) featuring:
  - Downloading or utilizing a simple SMS spam or equivalent textual classification array.
  - Applying Python `CountVectorizer` to generate vocabulary term frequencies.
  - Executing both models against the test matrix.
  - Visualization comparing accuracy scores or mapping words to highest likelihood metrics.
